# osrs-ge-prices

- Local env setup

```commandline
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

- Git lfs setup

```commandline
brew install git-lfs
git lfs install
```
